# postgres-docker

Postgres Docker Setup for example apps

## Usage

Copy the files to your project:

```
docker-compose.yml
postgres
  \migrations
  \Dockerfile
```

Then in your project folder, run:

```
docker-compose up
```

